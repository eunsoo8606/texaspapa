<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | Texas Papa 관리자
    </title>
    <link rel="icon" type="image/png" href="/images/logo.png">

    <!-- Google Fonts -->
    <link
        href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&family=Outfit:wght@400;500;600;700&display=swap"
        rel="stylesheet">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <!-- Admin Dashboard CSS -->
    <link rel="stylesheet" href="/css/admin-dashboard.css">

    <style>
        /* 삭제 버튼 스타일 데스크톱용 */
        .delete-btn-table {
            padding: 0.5rem 1rem;
            background: #fff5f5;
            color: #ff4757;
            border: 1px solid rgba(255, 71, 87, 0.3);
            border-radius: 6px;
            font-size: 0.85rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
            margin-left: 5px;
        }

        .delete-btn-table:hover {
            background: #ff4757;
            color: #ffffff;
            border-color: #ff4757;
            transform: translateY(-1px);
            box-shadow: 0 2px 4px rgba(255, 71, 87, 0.2);
        }

        /* 삭제 버튼 스타일 모바일용 */
        .delete-btn-mobile {
            width: 50px !important;
            height: 46px;
            /* view-btn-full 패딩 0.8rem에 맞춤 */
            display: flex !important;
            align-items: center;
            justify-content: center;
            background: #fff5f5 !important;
            color: #ff4757 !important;
            border: 1px solid rgba(255, 71, 87, 0.3) !important;
            border-radius: 8px !important;
            cursor: pointer;
            flex: none !important;
        }

        .delete-btn-mobile:hover {
            background: #ff4757 !important;
            color: #ffffff !important;
        }

        .view-btn,
        .view-btn-full,
        .view-btn:hover,
        .view-btn:focus,
        .view-btn-full:hover,
        .view-btn-full:focus {
            text-decoration: none !important;
            outline: none;
        }
    </style>
</head>

<body>
    <div class="admin-wrapper">
        <%- include('./partials/scripts') %>

            <!-- 사이드바 -->
            <%- include('./partials/sidebar', { user, currentPage }) %>

                <!-- 메인 콘텐츠 영역 -->
                <div class="main-content">
                    <!-- 헤더 -->
                    <header class="admin-header">
                        <div class="header-left">
                            <button class="sidebar-toggle" onclick="toggleSidebar()">
                                <i class="fas fa-bars"></i>
                            </button>
                            <h1 class="page-title">
                                <%= title %>
                            </h1>
                        </div>
                        <div class="header-right">
                            <div class="user-info">
                                <div class="user-avatar">
                                    <i class="fas fa-user-circle"></i>
                                </div>
                                <div class="user-details">
                                    <span class="user-name">
                                        <%= user.name || user.adminName %>
                                    </span>
                                    <span class="user-role">
                                        <%= user.role==='super_admin' ? '최고 관리자' : user.role==='admin' ? '관리자' : '편집자'
                                            %>
                                    </span>
                                </div>
                            </div>
                            <a href="/console/logout" class="logout-btn" title="로그아웃">
                                <i class="fas fa-sign-out-alt"></i>
                            </a>
                        </div>
                    </header>

                    <!-- 페이지 콘텐츠 -->
                    <main class="page-content">
                        <div class="board-container">
                            <div class="board-header">
                                <div class="board-info">
                                    <h2>가맹점 관리</h2>
                                    <p class="board-count">전체 <strong>
                                            <%= stores ? stores.length : 0 %>
                                        </strong>개</p>
                                </div>
                                <a href="/console/franchise/add" class="btn-primary"
                                    style="text-decoration: none; display: flex; align-items: center; justify-content: center;">
                                    <i class="fas fa-plus"></i>
                                    가맹점 추가
                                </a>
                            </div>

                            <div class="board-content">
                                <% if (stores && stores.length> 0) { %>
                                    <!-- 데스크톱 전용 테이블 -->
                                    <div class="board-table-wrapper desktop-only">
                                        <table class="board-table">
                                            <thead>
                                                <tr>
                                                    <th>매장명</th>
                                                    <th>연락처</th>
                                                    <th>주소</th>
                                                    <th>상태</th>
                                                    <th>관리</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% stores.forEach(store=> { %>
                                                    <tr class="board-row">
                                                        <td class="store-name-cell">
                                                            <%= store.name %>
                                                        </td>
                                                        <td>
                                                            <%= store.phone || '-' %>
                                                        </td>
                                                        <td>
                                                            <%= store.address %>
                                                        </td>
                                                        <td>
                                                            <span
                                                                class="status-badge <%= store.use_yn === 'Y' ? 'completed' : 'pending' %>">
                                                                <%= store.use_yn==='Y' ? '노출중' : '중지' %>
                                                            </span>
                                                        </td>
                                                        <td>
                                                            <div style="display: flex; gap: 5px;">
                                                                <a href="/console/franchise/edit/<%= store.id %>"
                                                                    class="view-btn">수정</a>
                                                                <button
                                                                    onclick="deleteStore(<%= store.id %>, '<%= store.name %>')"
                                                                    class="delete-btn-table">삭제</button>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                    <% }); %>
                                            </tbody>
                                        </table>
                                    </div>

                                    <!-- 모바일 전용 카드 리스트 -->
                                    <div class="mobile-card-list mobile-only">
                                        <% stores.forEach(store=> { %>
                                            <div class="inquiry-card">
                                                <div class="card-header">
                                                    <span
                                                        class="status-badge <%= store.use_yn === 'Y' ? 'completed' : 'pending' %>">
                                                        <%= store.use_yn==='Y' ? '노출중' : '중지' %>
                                                    </span>
                                                    <span class="card-date">
                                                        <%= store.phone || '연락처 없음' %>
                                                    </span>
                                                </div>
                                                <div class="card-body">
                                                    <div class="card-name">
                                                        <%= store.name %>
                                                    </div>
                                                    <div class="card-info-row">
                                                        <span><i class="fas fa-map-marker-alt"></i>
                                                            <%= store.address %>
                                                        </span>
                                                    </div>
                                                </div>
                                                <div class="card-footer"
                                                    style="display: flex; gap: 10px; align-items: stretch;">
                                                    <a href="/console/franchise/edit/<%= store.id %>"
                                                        class="view-btn-full"
                                                        style="flex: 1; text-decoration: none; display: flex; align-items: center; justify-content: center; margin: 0;">정보
                                                        수정</a>
                                                    <button onclick="deleteStore(<%= store.id %>, '<%= store.name %>')"
                                                        class="delete-btn-mobile"><i class="fas fa-trash"></i></button>
                                                </div>
                                            </div>
                                            <% }); %>
                                    </div>
                                    <% } else { %>
                                        <div class="empty-state">
                                            <i class="fas fa-store"></i>
                                            <p>등록된 가맹점이 없습니다.</p>
                                        </div>
                                        <% } %>
                            </div>
                        </div>
                    </main>
                </div>
    </div>
    <script>
        function deleteStore(id, name) {
            if (confirm(`'${name}' 매장을 정말 삭제하시겠습니까?`)) {
                const form = document.createElement('form');
                form.method = 'POST';
                form.action = `/console/franchise/delete/${id}`;
                document.body.appendChild(form);
                form.submit();
            }
        }
    </script>
</body>

</html>